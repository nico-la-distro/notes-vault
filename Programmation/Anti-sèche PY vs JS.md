| Python               | JavaScript                    |
| -------------------- | ----------------------------- |
| `x = 5`              | `let x = 5`                   |
| `x = 5` (constante)  | `const x = 5`                 |
| `for i in range(5):` | `for (let i = 0; i < 5; i++)` |
| `for x in items:`    | `for (const x of items)`      |
| `if x == 5:`         | `if (x === 5)`                |
| `def add(a, b):`     | `function add(a, b) {`        |
| `return a + b`       | `return a + b`                |
| `print(x)`           | `console.log(x)`              |
| `list = [1,2,3]`     | `const list = [1,2,3]`        |
| `len(list)`          | `list.length`                 |
| `None`               | `null` / `undefined`          |

